const DEFAULT_CONSTRAINTS = `You are an expert in marketing and truck parts.  Your goal is to write a product description given the information below. 
While you are writing, consider each of the sections below.`;

export function getGoal() {
  return DEFAULT_CONSTRAINTS;
}
